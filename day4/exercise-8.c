// Write a function that receives two numbers as arguments. The function should divide the first number by the second. Don't divide by the second number if it's zero. (Hint: Use an if statement.)
//
void divide_by(int a, int b)
{
    int result;
    if (b != 0) 
        result = a / b;
}
